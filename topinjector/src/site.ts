/*
  Канонический адрес сайта одной строкой.

  Он нужен как минимум в трёх местах: `metadataBase` рут-лэйаута собирает из
  него абсолютные ссылки метаданных, `sitemap.ts` — адреса страниц, а
  `robots.ts` — строку `Sitemap:`. Разъехавшийся между этими файлами хост даёт
  не ошибку сборки, а неверную выдачу: канонические ссылки одного вида,
  карта сайта другого.
*/
export const SITE_ORIGIN = "https://topinjector.ru";
